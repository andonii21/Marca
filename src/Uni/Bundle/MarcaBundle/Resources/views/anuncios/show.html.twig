{% extends '::base.html.twig' %}

{% block body -%}
    <h1>anuncios</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrenot</th>
                <td>{{ entity.nombreNot }}</td>
            </tr>
            <tr>
                <th>Textonot</th>
                <td>{{ entity.textoNot }}</td>
            </tr>
            <tr>
                <th>Companianot</th>
                <td>{{ entity.companiaNot }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('anuncios') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anuncios_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
